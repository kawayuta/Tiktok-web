<%= form_with(model: user_history, local: true) do |form| %>
  <% if user_history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_history.errors.count, "error") %> prohibited this user_history from being saved:</h2>

      <ul>
      <% user_history.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_official_id %>
    <%= form.text_area :user_official_id %>
  </div>

  <div class="field">
    <%= form.label :user_nick_named %>
    <%= form.text_area :user_nick_named %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
